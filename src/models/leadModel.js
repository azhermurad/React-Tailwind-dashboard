class LeadModel {
    constructor(title, description, buttonText) {
        this.title = title;
        this.description = description;
        this.buttonText = buttonText;
    }
}

export default LeadModel;
